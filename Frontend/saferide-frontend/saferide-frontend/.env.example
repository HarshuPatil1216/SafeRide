# Base URL of the SafeRide backend (no trailing slash, no /api suffix).
# Local dev default matches saferide-backend's application.properties (server.port=8081).
VITE_API_BASE_URL=http://localhost:8081
